import { info } from './index.types'
import { Ok } from './fn'

const Info:info = {
    name: 'Cool Testpack',
    author: 'Leopoldo Rezende',
    version: '0.0.1',
    description: 'Só na moral',
    ok: Ok
}

export default Info